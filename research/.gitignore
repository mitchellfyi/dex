# Workspaces (DX writes code here, ephemeral)
workspaces/

# Per-run results (only scores.tsv is tracked)
results/run-*/
results/latest

# Improvement proposals and applied-patch copies (generated, not final)
improvements/proposals/
improvements/applied/
orchestrate.log

# Local compatibility shims generated by config.sh
.tools/
